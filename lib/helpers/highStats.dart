import 'package:flutter/material.dart';

class HighStats extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Special Stats'),
      ),
    );
  }
}
